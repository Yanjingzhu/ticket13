name: actcheck
on: [push]
  
jobs:
  actcheck:
    runs-on: [ubuntu-latest]
    steps:
      - name: checkout
        uses: actions/checkout@develop
        with:
          repository: 'weide-zhou/ticket18'
      - name: install act
        run: |
          curl https://raw.githubusercontent.com/nektos/act/master/install.sh | sudo bash
      - name: act
        run: |
          act -l
          act pull_request
          
