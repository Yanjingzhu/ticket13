name: ifcondition
on: [push]
  
jobs:
  job1:
    runs-on: [ubuntu-latest]
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: checkout
        uses: actions/checkout@v2
      - name: hashfiles
        run: |
          echo ${{ hashFiles('testfolder1/**') }}
#      - name: skip step
#        if: contains(hashFiles('testfolder1/**')
