name: conditional job
on: [push]
jobs:
  conditionaljob:
    runs-on: [ubuntu-latest]
    steps:
          
      - name: getstatus
        run: |
          fail
      - name: Dump job context
        if: always()
        env:
          JOB_CONTEXT: ${{ toJson(job) }}
        run: |
          echo $JOB_CONTEXT
          echo ${{ job.status }}
