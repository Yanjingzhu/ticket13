name: nodesetup
on: [push]
  
jobs:
  nodesetup:
    runs-on: [ubuntu-latest]
    steps:
      - name: nodesetup
        uses: actions/setup-node@v1.4
        with:
          node-version: 13
      - name: npmversion
        run: |
          npm version
