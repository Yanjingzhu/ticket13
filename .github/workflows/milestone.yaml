name: bashcheck
on: [push]
  
jobs:
  bashcheck:
    runs-on: [windows-latest]
    steps:
      - name: Fix up git URLs
        run: echo -e '[url "https://github.com/"]\n  insteadOf = "git@github.com:"' >> ~/.gitnotconfig
        shell: bash
          
