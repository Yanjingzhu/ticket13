name: "Dogfood"

on:
  issues:
    types: ["opened"]

jobs:
  pvscbot:
    runs-on: ubuntu-latest
    steps:
    - uses: docker://brettcannon/pvscbot:latest
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
