name: testjoin
on: [push]
jobs:
  testjoin:
    runs-on: [ubuntu-latest]
    steps:
      - name: testjoin
        env:
          JOINTEXT: ${{ join('Hello', 'world!') }}
        run: |
          echo "$JOINTEXT"
